# Messages TODO

- [x] Match the user messages layout closer to the provided reference.
- [x] Replace `Agent Alice` / `Demo Agent` labels with `Zeni Agent`.
- [x] Show support/admin conversation label as `Zeni Admin`.
- [x] Add a clear active-row indicator in inbox (left black bar + tinted background).
- [x] Use white inbound bubbles and black outbound bubbles in thread view.
- [x] Use `Zeni Agent` / `Zeni Admin` in user-facing typing and bootstrap copy.
- [x] Split support/admin into separate backend identities and separate default threads (Zeni Support, Zeni Agent, Zeni Admin).
